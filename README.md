# OmniSynch Inventory System

## Estructura
- `/css`: Estilos visuales.
- `/js`: Lógica modular (API, UI, App).
- `/assets`: Imágenes y recursos.

## Configuración n8n
Para que el sistema funcione, abre `js/api.js` y reemplaza `BASE_URL` con la URL de tu instancia de n8n.